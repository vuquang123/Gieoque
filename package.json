{
  "name": "gieoque",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo Run 'vercel dev --yes --listen 3000' directly to avoid recursion",
    "vercel-dev": "vercel dev --yes --listen 3000"
  },
  "dependencies": {
    "google-auth-library": "^9.14.1"
  }
}
